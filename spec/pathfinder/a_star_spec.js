/*global describe, expect, it, beforeEach, $, _, pathfinder */
describe("AStar", function () {
    'use strict';
    var map;

    beforeEach(function () {
        map = pathfinder.map(10, 10);
    });
});